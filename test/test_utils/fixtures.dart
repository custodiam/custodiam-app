// Factory functions for entities used across tests.
//
// Add factories here as entities are introduced in features/ (e.g.
// makeVoluntario(), makeServicio()). Keep arguments optional with
// sensible defaults so tests can override only the relevant fields.
//
// See guide 22 §5 (test conventions) for the patterns.
//
// Currently empty — first entities will arrive with EN-01-02 (auth)
// and EN-02-xx (voluntarios).
