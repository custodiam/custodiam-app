// Shared mocktail mocks for cross-feature collaborators (ApiClient,
// TokenStore, AnalyticsService, etc.) and the registerFallbacks()
// helper that prepares mocktail for non-trivial argument types.
//
// See guide 22 §5 for the conventions and §3 for what gets mocked
// per layer.
//
// Currently empty — the first shared mock will be MockApiClient when
// the auth Repository tests land in EN-01-02.
